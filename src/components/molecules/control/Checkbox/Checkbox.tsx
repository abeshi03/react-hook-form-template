// - フレームワーク =======================================================================================================
import React, { memo, VFC } from "react";

// - アセット ============================================================================================================
import styles from "./Checkbox.module.scss"


/* eslint-disable-next-line react/display-name */
export const Checkbox: VFC = memo((props) => {

  // const {$END$} = props;

  return (
    null
  );
});
